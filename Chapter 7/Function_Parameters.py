# Function calling with arguments

# Function definition with parameters
def average(a,b):
    avg=(a+b)/2
    print(f"Num1 = {a} \nNum2 = {b} \nAverage = {avg}")

# Calling functions with arguments
average(10,20)
average(5,7)
